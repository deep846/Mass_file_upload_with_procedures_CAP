_schema-version: 3.3.0
ID: massUpload
description: A simple CAP project.
version: 1.0.0
modules:
  - name: massUpload-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: massUpload-db
      - name: uaa_massUpload
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: massUpload-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: massUpload-db
    parameters:
      buildpack: nodejs_buildpack
  - name: massUpload-destination-content
    type: com.sap.application.content
    requires:
      - name: massUpload-destination-service
        parameters:
          content-target: true
      - name: massUpload_html_repo_host
        parameters:
          service-key:
            name: massUpload_html_repo_host-key
      - name: uaa_massUpload
        parameters:
          service-key:
            name: uaa_massUpload-key
    parameters:
      content:
        instance:
          destinations:
            - Name: massupload_massUpload_html_repo_host
              ServiceInstanceName: massUpload-html5-app-host-service
              ServiceKeyName: massUpload_html_repo_host-key
              sap.cloud.service: massupload
            - Authentication: OAuth2UserTokenExchange
              Name: massupload_uaa_massUpload
              ServiceInstanceName: massUpload-xsuaa-service
              ServiceKeyName: uaa_massUpload-key
              sap.cloud.service: massupload
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
  - name: massUpload-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: massUpload_html_repo_host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - massuploadexcel.zip
          name: massuploadexcel
          target-path: resources/
  - name: massuploadexcel
    type: html5
    path: app/massuploadexcel
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: massUpload-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: massUpload-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
              - Authentication: NoAuthentication
                HTML5.DynamicDestination: true
                HTML5.ForwardAuthToken: true
                Name: srv-api
                ProxyType: Internet
                Type: HTTP
                URL: ~{srv-api/srv-url}
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: massUpload-destination-service
      service-plan: lite
    requires:
      - name: srv-api
  - name: massUpload_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: massUpload-html5-app-host-service
      service-plan: app-host
  - name: uaa_massUpload
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        oauth2-configuration:
          credential-types:
            - binding-secret
            - x509
        tenant-mode: dedicated
        xsappname: massUpload-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-name: massUpload-xsuaa-service
      service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
